export type FilmButtonType = {
  title: string;
  year: string;
  poster: string;
  id: string;
};

export type FilmRatingType = {
  name: string;
  rating: string;
};

export type FilmFeatureType = {
  feature: string;
  details: string;
};

export type ApiRatingType = {
  Source: string;
  Value: string;
};
